# ── Node / Next.js ───────────────────────────────────────────
node_modules/
.next/
.turbo/
out/
coverage/
*.tsbuildinfo

# ── Vercel ────────────────────────────────────────────────────
.vercel/

# ── Environment & secrets ─────────────────────────────────────
.env
.env.*
!.env.example

# ── AI / local tooling ────────────────────────────────────────
.claude/
.copilot-kit/
AudioInstructions/
Demo-01/
copilot-starter-kit/
.env.copilot.example
PROJECT-ADR.md

# ── Logs & debug output ───────────────────────────────────────
*.log
*.trace
*.dmp
*.dump

# ── Database files ────────────────────────────────────────────
*.sqlite
*.sqlite3

# ── OS / editor artifacts ─────────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini
*.swp
*.swo

# ── Secret / credential file types ───────────────────────────
*.pem
*.p12
*.pfx
*.p8
*.key
*.keys
*.crt
*.cer
*.jks
*.keystore
id_rsa
id_dsa
id_ecdsa
id_ed25519
*.secret
*.secrets
*.token
*.tokens
*.credential
*.credentials
service-account*.json
client_secret*.json

# ── Terraform & infra state ───────────────────────────────────
*.tfstate
*.tfstate.*

# ── Cloud / tool credentials ─────────────────────────────────
.npmrc
.netrc
.pypirc
.git-credentials
.aws/credentials
.aws/config
.kube/config
.gcloud/
